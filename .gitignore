# SSH keys and certificates
.ssh/
**/*_rsa
**/*_rsa.pub
**/*_dsa
**/*_dsa.pub
**/*_ecdsa
**/*_ecdsa.pub
**/*_ed25519
**/*_ed25519.pub
**/*.pem
**/*.key
**/*.p12
**/*.pfx

# GPG keys
.gnupg/

# Environment files (may contain API keys, tokens, passwords)
.env
.env.*

# Password managers and credential stores
.password-store/
.keepass/
**/*.kdbx

# Browser profiles (may contain saved passwords, cookies, history)
.mozilla/
.config/google-chrome/
.config/chromium/
.config/BraveSoftware/

# Shell history files (may contain commands with credentials)
.bash_history
.zsh_history
.zsh_sessions/
.python_history
.lesshst
.mysql_history
.psql_history
.sqlite_history

# Private configuration with credentials
.netrc
.authinfo
.authinfo.gpg

# Cloud provider credentials
.aws/
.azure/
.config/gcloud/
.kube/

# Wallet / keyring data
.local/share/keyrings/
